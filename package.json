{
  "name": "smart-water-analytics",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "mock": "python scripts/mock_data_generator.py",
    "build": "node frontend/build.cjs",
    "serve": "npx serve frontend/dist -l 5173",
    "agent": "cd agent && python server.py",
    "demo": "npm run mock && npm run build && npm run serve",
    "demo:full": "npm run mock && npm run build && echo 'Start agent: cd agent && python server.py' && npm run serve"
  },
  "dependencies": {
    "xlsx": "^0.18.5"
  }
}
